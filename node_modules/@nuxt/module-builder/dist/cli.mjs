#!/usr/bin/env node
import mri from 'mri';
import { resolve } from 'pathe';
import { b as buildModule } from './shared/module-builder.4bb3ed8c.mjs';
import 'fs';
import 'url';
import 'consola';
import 'mlly';

async function main() {
  const args = mri(process.argv.slice(2));
  const rootDir = resolve(args._[0] || ".");
  await buildModule({
    rootDir,
    outDir: args.outDir,
    stub: args.stub
  });
}
main().catch((err) => {
  console.error(err);
  process.exit(1);
});
